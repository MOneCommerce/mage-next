// export const formatNumberAsMoney = (amount = 0, currency = "USD") =>
// 	new Intl.NumberFormat(locale, {
// 		style: "currency",
// 		currency,
// 	}).format(amount);

// export const formatNumberByLocale = (amount = 0, locale = DEFAULT_LOCALE) =>
// 	new Intl.NumberFormat(locale).format(amount);

// export const formatTimeByLocale = (
// 	time: string | number | Date,
// 	locale = DEFAULT_LOCALE
// ) => new Date(time).toLocaleDateString(locale);

// export const formatDateByLocale = (
// 	time: string | number | Date,
// 	locale = DEFAULT_LOCALE
// ) => new Date(time).toLocaleTimeString(locale);
